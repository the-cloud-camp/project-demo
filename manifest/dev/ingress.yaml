apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: 
  labels:
    name: 
spec:
  rules:
  - host: tcc-01.th1.proen.cloud
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: kai-nuxtjs
            port: 
              number: 3000

name: "🚓 Test Context"
# Only run on schedule and PR because it's really slow

on:
  push:
    branches: [ "feature/*" ]
  workflow_dispatch: # allow manual runs

jobs:
  test-context:
    name: test echo context
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
      security-events: write

    steps:
      - name: Context Show
        run: |
            echo "HELLO=hello" >> $GITHUB_ENV
            source $GITHUB_ENV
            echo "$HELLO"
